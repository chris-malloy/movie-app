<!DOCTYPE html>
<html lang="en">
    <head>
        <title></title>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <% include head %>
        <link href="css/style.css" rel="stylesheet">
    </head>
    <body>
    <% include navbar %>
    <div class="container">
        <div class="row">
            <div class="col-sm-12">
				<% for(let i = 0; i < parsedData.length; i++){ %>
					<div class="col-sm-3">
						<a href="/movie/<%savedMovies[i].id%>">
							<img src="<%= imageBaseUrl + savedMovies[i].poster_path %>" />
						</a>
					</div>
				<% } %>
			</div>
        </div>
    </div>
    
    </body>
</html>